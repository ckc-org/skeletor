import { extendTheme } from "native-base"

const breakpoints = {
  base: 0,
  sm: 480,
  md: 768,
  lg: 992,
  xl: 1280,
}

export const colors = {

}

export const theme = extendTheme({
  colors,
  breakpoints,
  components: {
  }
})
