### MonstaDex

### installation

```bash
$ cp .env_sample .env
$ docker-compose up -d
# you will need to migrate, then create a super user! 
# can now view http://localhost or http://localhost:8000
```
