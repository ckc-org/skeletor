# frontend dev problems

Some common frontend dev problems and fixes

### `localhost:3000` gives strange errors or never loads

Try running these command in builder:
```bash
$ npm run generate
``` 

Then see if it's loading fine in the browser...
